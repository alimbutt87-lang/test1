import React from 'react'
import ReactDOM from 'react-dom/client'
import InterviewSimulator from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <InterviewSimulator />
  </React.StrictMode>,
)
